import 'package:flutter/material.dart';

/// A class that holds custom text styles for the application.
class AppTextStyles {
  /// Style for the large counter number.
  static const TextStyle counterNumber = TextStyle(
    fontSize: 80,
    fontWeight: FontWeight.bold,
  );

  /// Style for the label below the counter number (e.g., "clicks").
  static const TextStyle counterLabel = TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.w300,
  );
}
